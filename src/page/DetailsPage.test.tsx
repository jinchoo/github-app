import React from 'react';
import { render, screen, fireEvent, waitFor } from '@testing-library/react';
import userEvent from '@testing-library/user-event';

describe('DetailsPage', () => {
  test.todo(
    'when page load, should do api to get the repo info from github api'
  );
  test.todo('should display the repo name');
  test.todo('should display the repo descripotion');
  test.todo('should display the repo stars');
});
